---
spring:
  profiles:
    active: dev
  application:
    name: greeting
  cloud:
    config:
      uri: http://localhost:8001
server:
  port: 8002

---
spring:
  profiles:
    active: docker
  application:
    name: greeting
  cloud:
    config:
      uri: http://config-server:8001
server:
  port: 8002

---
spring:
  profiles:
    active: default
  application:
    name: greeting
  cloud:
    config:
      uri: http://localhost:8001
server:
  port: 8002
