---
spring:
  profiles:
    active: dev
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8001
server:
  port: 8761

---
spring:
  profiles:
    active: docker
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://config-server:8001
server:
  port: 8761

---
spring:
  profiles:
    active: default
  application:
    name: eureka-server
  cloud:
    config:
      uri: http://localhost:8001
server:
  port: 8761
